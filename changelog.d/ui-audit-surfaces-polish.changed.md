Surface and empty-state polish. Dialogs share one background (and Startup App edit follows theme switches); System Logs, Hosts, generic tables, the Homebrew app-updates table and the File Shredder staging list use the same card surface instead of pure white or square boxes; empty states use the themed icon set and one heading style instead of colour emoji; "Not Found Installed Packages" reads "No installed packages found". Each screen has a single primary action again (Disk Tools, Boot Analysis, Settings), Boot Analysis shows uptime as days/hours/minutes rather than raw seconds, Cache Rebuild buttons no longer overlap their card, and Search's loading indicator follows the theme.
