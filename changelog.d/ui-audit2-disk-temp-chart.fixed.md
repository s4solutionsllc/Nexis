The Disk Temperature history on the Resources page now shows a line. Disk health is only read every 30 seconds, but the chart advanced one slot per reading against a 60-slot axis, so it stayed blank for the first minute and took half an hour to fill; it now advances once a second holding the latest reading, keeps collecting while the page is hidden, and uses a 20–90 °C scale instead of 0–100 so changes are visible.
