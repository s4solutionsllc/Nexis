Fixed several wrong readouts found during the UI audit: Helpers → Host Manage opened empty until its tab was clicked (it now loads the hosts file when the page is first shown); Hardware Info listed every synthesized APFS container as a separate drive with duplicate SMART data, and showed battery charge percentages labelled as mAh on newer macOS (real mAh figures are now read from BatteryData); and the Resources disk chart could show "16777216 TiB/s" and lock its axis at that scale when an I/O counter stepped backwards.
