Helpers tools (Firewall, Open Ports, Network Diagnostics, Cache Rebuild, Snapshots, Tweaks, Wake-on-LAN and the Linux tuning panels) now use the same cards, titles, buttons and table style as the rest of the app instead of their own white 4px variants. The Processes table gives its spare width to the Process column (command lines were cut to "/System/…" while the kill-icon column took the room), shows the full "Resident Memory" header and a legible ✕, and Services descriptions use the available width instead of truncating after a dozen characters.
